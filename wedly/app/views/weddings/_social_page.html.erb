<h3> This is your Social Page.</h3>
<h4> Think about it as your Pre-Wedding Scrapbook to share with Friends and Family! You can share photos and they can add their wishes and comments for your future together.</h4>
<% if current_user == @wedding.user %>
<% end %>

<p>Already Added Notes</p>
<% @wedding.comments.each do |comment| %>
  <p>
    <%= comment.note %>
    <br>
    <% if comment.user != nil %>
      <em> by <%= comment.user.first_name %></em>
      Added on: <%= comment.created_at %>
    <% end %>
  </p>
  <%= link_to "Edit Note", " " %> |
  <%# link_to "Remove Note", wedding_comment_path(@wedding, comment), method: :delete, data: {confirm: "Are you sure you want to delete this Guest?"} %>

  <% if current_user %>
    <h4>Add some love notes to this wedding! </h4>
    <%= form_for([comment.wedding, comment]) do |f| %>
      <% if @comment.errors.any? %>
        <div id= "error_explanation">
          <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this note from being saved: </h2>
          <ul>
          <% @comment.errors.full_messages.each do |msg| %>
            <li><%= msg %> </li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class= "field">
        <%= f.label :note %><br />
        <%= f.text_area :note %>
      </div>
      <div class = "actions">
        <%= f.submit %>
      </div>
    <% end %>

    <% else %>
    <p>Please <%= link_to "Log In", new_user_session_path %> to add a note. </p>
  <% end %>
<% end %>
















