<%= form_for([@invitation.wedding, @invitation]) do |f| %>
  <div class="field">
    <%= f.label :recipient_email %><br/>
    <%= f.text_field :recipient_email %>
  </div>
  <div class="action">
    <%= f.submit 'Invite' %>
  </div>
<% end %>