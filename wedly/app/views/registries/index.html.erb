<br/>
<br/>
<h2>All Registries for <%= @wedding.partner_1 %> &#38 <%= @wedding.partner_2 %></h2>
<!-- All Registries -->
<% @registries.each do |registry| %>
  <p><strong>Store: </strong><%= registry.store %></p>
  <p><strong>Link: </strong>
  <a href="<%= registry.link_to_registry %>" target= "_blank"><%= registry.link_to_registry %></a></p>
  <br/>

<!-- Edit for Registries -->
  <% if current_user == @wedding.user %>
    <%= link_to "Edit", edit_wedding_registry_path(@wedding, registry) %> |
    <%= link_to "Delete", wedding_registry_path(@wedding, registry), method: :delete, data: {confirm: "Are you sure you want to delete this Registry link?"} %>
  <% end %>
<% end %>
<br/>
<h4>Create a New Registry</h4>
<%= form_for([@wedding, Registry.new]) do |f| %>
  <%= f.hidden_field :wedding_id, value: @wedding.id %>
    <div class="field">
      <%= f.label "Store:" %>
      <%= f.select(:store, options_for_select([["Hudson Bay", "Hudson Bay"], ["Bath & Beyond", "Bath & Beyond"], ["Crate & Barrel", "Crate & Barrel"], ["Pottery Barn", "Pottery Barn"], ["Willams-Sonoma", "Willams-Sonoma"], ["Pottery Barn", "Pottery Barn"], ["Other", "Other"]]))%>
    </div>

    <div class= "field">
      <%= f.label "Link to Registry:" %>
      <%= f.text_field :link_to_registry %>
    </div>
  <br/>
   <div class="actions">
    <%= f.submit %>
  </div>
<% end %>